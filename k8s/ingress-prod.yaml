apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-nextjs-portfolio-prod
  namespace: nextjs-portfolio
  annotations:
    kubernetes.io/ingress.class: "haproxy"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  tls:
  - hosts:
    - codecrafter.space
    secretName: codecrafter-space-tls-prod
  rules:
  - host: codecrafter.space
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: service-nextjs-portfolio
            port:
              number: 3000